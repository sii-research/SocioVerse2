# paper-search-pro runtime dependencies.
# All libraries listed here are pip-installed at runtime — none are
# vendored in-tree. The only vendored content lives under
# `scripts/vendored/` (currently: tenacity_retry.py + paper-qa
# attribution files); see `scripts/vendored/README-vendored.md`.

# OpenAlex client (L1 primary source).
pyalex>=0.21

# Semantic Scholar client (L3 enrichment: influentialCitationCount + abstract fallback).
semanticscholar>=0.10

# PubMed via NCBI Entrez (L2 medical: MeSH enricher / search).
biopython>=1.85

# arXiv API client (L2 freshness: T-0~T-4 preprint sentinel).
arxiv>=2.1

# HTTP client (CrossRef polite-pool calls).
requests>=2.32

# YAML config loader (~/.paper-search-pro/config.yaml).
PyYAML>=6.0

# Template engine for Markdown report rendering.
Jinja2>=3.1
